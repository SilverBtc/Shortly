# Dépendances Python du daemon Qwen3-TTS (sans PyTorch).
#
# PyTorch s'installe séparément selon le matériel :
#   - GPU AMD (ROCm)  : pip install -r requirements-gpu.txt
#   - CPU             : pip install -r requirements-cpu.txt
#   - (GPU NVIDIA)    : pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128

transformers==4.57.3
accelerate==1.12.0
fastapi==0.141.1
uvicorn==0.52.4
numpy==2.4.6
soundfile==0.14.0
httpx==0.28.1
librosa==0.11.0
